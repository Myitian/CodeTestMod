{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${mod_version}",
  "name": "${mod_name}",
  "description": "${mod_description}",
  "authors": [
    "${mod_author}"
  ],
  "contact": {
    "sources": "${mod_source}"
  },
  "license": "${mod_license}",
  "icon": "pack.png",
  "environment": "client",
  "mixins": [
    "${mod_id}-common.mixins.json"
  ],
  "entrypoints": {
    "client": [
      "${maven_group}.fabric.CodeTestFabric"
    ]
  },
  "depends": {
    "minecraft": "${supported_minecraft_version_fabric}"
  }
}